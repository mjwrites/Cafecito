publico clase test {
    publico estatico vacio main(String[] args) {
    cara[] Array8 = nuevo cara[5];

    para (ent i = 0; i < Array8.length;i++ ) {
        si (Array8[i] >= 90)
            Array8[i] = 'A';
        entonces si (Array8[i] >= 80)
            Array8[i] = 'B';
        entonces si (Array8[i] >= 70)
            Array8[i] = 'C';
        entonces si (Array8[i] >= 60)
            Array8[i] = 'D';
        entonces
            Array8[i] = 'F';
    }
}
}
